import { cn } from '@/lib/utils';
import { ReactNode } from 'react';

  className?: string;
  glowColor?: string;

  children,
  title,
}

        <div className="mb-4
        </d
      <div c
      </
  );

  children
  title?: string;
  titleColor?: st

  children,
  title,
  titleC
  return (
      {title && (
          {t
      )}
    
 
  );
}
