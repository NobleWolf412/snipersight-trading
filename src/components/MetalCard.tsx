import { cn } from '@/lib/utils';


}
export function MetalC
  className
}

export function MetalCard({ 
  children,
  className
}: MetalCardProps) {
  return (
    <div className={cn(
      'relative bg-card border border-border rounded-xl p-6 backdrop-blur-sm shadow-lg',
      className
  title
      {children}
  glowColo
  );
e

interface MetalSectionProps {
  children: ReactNode;
        <h2 class
          titleColor,
        )}>
        </h2>
 

  );































