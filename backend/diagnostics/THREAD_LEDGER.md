# SniperSight — Thread Ledger (living status board)

The single place that answers "what have we found, and where does each thing stand?"
Decisions/ holds the deep per-topic write-ups; memory holds one-liners; the debrief
re-surfaces live flags. THIS file is the kanban: every thread, its status, and the ONE
next action — so nothing gets validated-then-dropped when we jump to the next problem.

**Update rule:** when a thread changes state, edit its row here in the SAME commit as the
work. Status: 🔴 OPEN · 🟡 IN-VALIDATION (measuring before fixing) · 🟠 NEEDS-TRACE
(mechanism unconfirmed) · 🟢 SHIPPED+VERIFIED · ⚫ REFUTED/CLOSED.
Tier = CLAUDE.md triage (1 BROKEN · 2 RISKY · 3 CORRECTNESS · 4 FRICTION · 5 NICE).

Last updated: 2026-06-06 (after session f193dd54, n=29; clean post-clamp corpus now n=138).

---

## 🔴 OPEN — found, validated or clear, NOT yet fixed (the "abc we left behind")

| ID | Thread | Tier | Evidence | NEXT ACTION |
|----|--------|------|----------|-------------|
| T1 | **MACD Veto "ANTI" was a FALSE ALARM (confound) — DO NOT remove** | 3 | Investigated 2026-06-05: r_out −0.15 ALL-data → **+0.02 post-clamp** (confound, not anti-predictive). And it's a binary VETO GATE (scorer.py:2790-2799 score 0/100; :3206 score<50 hard-blocks APEX/A), so among taken trades it's 100±0 by construction — r_out is range-restricted/uninterpretable, not "dead." Removing the gate would let MACD-opposed trades through with zero evidence it helps. **Keep it.** | **DIAGNOSTIC FIX SHIPPED:** factor_contribution now judges ANTI/KEEP on the CLEAN post-clamp window (CLAMP_DATE 2026-05-31), labels VETO_FACTORS as "GATE (r_out not interpretable)", and flags CONFOUND when full-history signal vanishes on clean data. MACD Veto now correctly reads GATE (not ANTI). Side-effect: de-confounding surfaced two clean-ANTI candidates the full-history view had MASKED — now tracked as **T10**. |
| T2 | **Inert/constant factors** — Weekly StochRSI always 50; Institutional Sequence / Multi-Candle / Nested OB pegged near-max (std≈0). ~20 weighted pts of no-discrimination padding inflating every score | 4 | factor_contribution.py (4–6 "dead" each run) | Trim/zero-weight after a tune-confluence-weights pass; re-baseline + symmetry-guard + §16. Hygiene, not edge. NOT started. |
| T3 | **SMC TF/mode routing — F4/F5/F6 open** (~~F1~~✅ ~~F2~~✅ SHIPPED; F3 DEFERRED-documented; F4/F5 correctness; F6 friction) | 2 | decisions/2026-05-31__smc-tf-mode-routing-audit.md | **F2 FIXED** (HTF-swing stop fallback now intersects htf_swing_allowed ∩ stop_timeframes via `_resolve_htf_swing_allowed` — closes the intraday-tier 4h-swing wide-stop pathway; symmetry-guard PASS / backend-integrity CLEAN / §16 14/14; test_htf_swing_stop_timeframes_constraint.py). **F3 DEFERRED** (documented): rerouting cascade "intraday"→aggressive has real blast radius — the "intraday" string is overloaded (also the generic unknown-type fallback planner_service.py:222, and test_intraday_config_is_balanced asserts intraday→balanced as INTENDED). F2's fix closes the actual wide-stop risk regardless of config routing, so F3 (min_rr/buffer deltas, not the wide stop) is low-value/higher-risk. NEXT: F4 (_calculate_targets ignores target_timeframes) / F5 (bar-counts not TF-scaled) / F6 (friction). |
| T4 | **Cascade scalp monoculture + compressed-regime underperformance** — bot trades ~93.5% scalp; global "compressed" regime saturation strips swing | 3 | orchestrator.py:2347-2358; memory project_cascade_scalp_monoculture. **NEW (edge_by_regime.py, clean post-clamp):** down_normal +1.70/tr (swing +2.27, n=34) vs down_compressed −0.09/tr (scalp −0.27, n=73) — a CLEAN (un-confounded, both post-clamp) hint that the bot gets chopped in compressed/choppy downtrends and does better in normal-vol trends. Modest n + gross (likely net-breakeven after fees). | Bootstrap the down_normal-vs-down_compressed gap (edge_significance) before acting; if it holds, a compressed-regime de-rate / scalp-in-compressed gate is the lever. Still NOT started. |
| T5 | **Stops land inside SMC liquidity pools** (PWL/PDH/EQL) → systematically swept | 2 | memory project_stop_placement_pwl_proximity (deferred) | Audit risk_engine stop placement vs liquidity map. NOT started. |
| T11 | **Long-only WCL/DCL cycle bypass — RESOLVED as documented (2026-06-06)** | 3 | Investigated: CycleContext (smc.py:627-628) models cycle LOWS only — NO in_wch_zone/in_dch_zone. So a SHORT mirror has no data to key on (would need building cycle-HIGH detection), AND a mirror is the WRONG direction (it would admit MORE counter-trend shorts = the net-losing cohort). symmetry-guard SUSPECT CLOSED via rationale comment at scorer.py:~226. | RESIDUAL (low pri): does the long bypass earn its keep at all? Measure WCL/DCL-bypassed long outcomes — bypass is debug-logged, not journaled, so needs log/telemetry mining. Not blocking. |
| T13 | **ATR-regime classification tests — FIXED (stale test, not a regression)** | 2 | RESOLVED 2026-06-06: the classifier is CORRECT — `_detect_volatility` uses the DAILY-calibrated %-ATR bands (<2.5 calm/<5 normal/<7 elevated/≥7 explosive, the §10 standing fix). The TEST still asserted the RETIRED 5-min bands (0.8/1.5/2.5/4.0 — explicitly deprecated at regime_detector.py:750-757). Updated expectations to current bands + capped the fixture stop at 2% so the high-ATR (explosive) case clears the 3% scalp cap. All 6 pass. Test-only, no production behavior changed. | None — closed. (The stale test had been masking the standing-fix surface with a red mark.) |
| T12 | **Clamp-on-capped-wide-stop scalp → RR ≈ 1.0 (geometry quality)** — when a scalp's structural stop is capped to 1.5 ATR AND TP1 is reachability-clamped, the resulting RR collapses to ~1.0 (SHIB f193dd54: capped 1.5 ATR + tp1_clamped → RR 1.04). TP1 becomes reachable at the cost of edge. | 4 | /trade-autopsy SHIB 2026-06-06 (RR 1.04, conv C, lost). Pattern, n=1 so far. | Watch: count clamped-AND-capped scalps + their realized RR over more sessions; if systematic, the clamp/cap interplay needs a min-RR floor (don't take a clamped scalp whose post-clamp RR < ~1.3). NOT actioned. |

| T17 | **Position caps count SLOTS, not CORRELATION — N correlated same-dir alt-shorts = one leveraged bet** | 2 (RISKY) | Session 1993dcd5: 9 SHORTs on correlated alts; **6 of 8 stops fired in a 58-sec spike (22:14:28–22:15:26 UTC)** — one counter-trend candle erased the whole book (−54.73). `backend/bot/executor/position_manager.py` enforces max_positions (slot count) + per-scan directional cap, but has NO correlated-exposure guard → "max 10 positions" gave the ILLUSION of diversification over what was effectively one bet. Most actionable SAFETY lever found this arc. | Design a correlation/cluster exposure cap (treat correlated same-direction alts as ONE risk unit; cap per cluster, not per symbol). Risk/execution change → Plan agent + symmetry-guard + blast-radius + §16; NOT a drop-in. **Tonight's rest_maker run is EXPOSED (could repeat).** Tier-2 RISKY outranks the data-gathering/strategic leads by triage → tomorrow's #1 candidate. NOT started. |
| T15 | **Recorded `risk_reward_ratio` is PLANNED-not-realized on snapped trades — poisons RR analytics** | 3 (CORRECTNESS) | AVAX trade-autopsy (89db3a85, 2026-06-05): planned entry 7.07/stop 7.22/target 5.75 → RR logged **8.818** (planned math, risk 0.15); snap filled @ 6.673 → REAL risk 0.547 (3.6× wider), REAL RR **1.69**. `stop_distance_atr` recorded on ACTUAL entry (1.50) but RR on PLANNED entry → row internally inconsistent for every snapped (snap_taker) trade. ~all history is snap_taker → conviction tiers, factor-edge RR signal, autopsy RR columns all inflated. Does NOT affect rest_maker (snap-free). | Journal REALIZED RR (from actual fill) OR tag `rr_is_planned`. Touches journaling/CompletedTrade → backend-integrity + §16. Low urgency now (maker experiment is snap-free) but it has silently biased the whole RR corpus. NOT started. |

| T19 | **Macro/dominance engine PINNED to STABLE_SCARE by stale absolute thresholds → permanent bearish bias on EVERY trade (root of T17 concentration)** | 2 (RISKY) → arguably #1 | CONFIRMED 2026-06-08. `macro_context.py` is a full 9-state dominance engine (RISK_ON/OFF, ALT_SEASON, BTC_ONLY_RALLY, STABLE_SCARE, CORRELATED_SELLOFF…) implementing the operator's original "dominance structure → direction" idea. BUT it's jammed on ONE state: **168/168 post-clamp trades = STABLE_SCARE.** Root: Layer-0 override `stable_dom > 6.0 → STABLE_SCARE` (`macro_context.py:116`) fires unconditionally because LIVE `stable_dom=13.92` (raw 0-100 %, `dominance_service.py:43`). ALL absolute thresholds are ~2021-calibrated and now stale: live `btc_dom=64.55` (>58 always true; <50 RISK_ON path never fires). 8 of 9 states are DEAD CODE. **AND the overlay applying this bias is silently ALWAYS-ON for the bot** — `macro_overlay_enabled` core default flipped to `True` (`defaults.py:40`), applied at `scorer.py:3114`; the manual /scan endpoint still defaults it `False` (`scanner.py:508/593`), so the operator's old toggle is now buried + on-by-default for the bot path. STABLE_SCARE is NOT neutral — `compute_macro_score:297-304` applies LONG −3 / SHORT +2 / **alt-SHORT +1** on every trade. Consequences (measured): corpus 71% short (120/48), LONG −0.51/tr vs SHORT +0.66/tr (longs fight the −3), and the **+1 alt-short nudge is the ROOT of the −54 alt-short concentration (T17 caps the symptom, this is the cause)**. Also neuters the operator's edge idea — "does dominance predict direction" is UNTESTABLE with zero state variation. TIME BOMB: "accidentally right" only because market fell; when alts recover the pinned engine shorts INTO it (systematized T16). | **Recalibrate (gated: Plan → symmetry-guard → §16 + baseline doc — baseline now definitive: btc_dom 64.5 / stable_dom 13.9 vs thresholds 58/6).** Fix direction: DROP stale absolute thresholds, lean on RELATIVE/VELOCITY (dominance *rising fast* = risk-off) since absolute levels drift secularly → any fixed number re-stales. This is also the prerequisite to EVER testing the dominance-direction edge idea (T8's "new predictive input"). CAVEAT: unsticking removes the silent bias + enables the test but does NOT guarantee edge. Touches scorer/regime macro overlay → standing-fix "real dominance data" (data is REAL, threshold is stale — calibration bug not data bug). NOT started. |

| T20 | **FVG detector BLIND to ~100% of standard fair value gaps — spurious "middle-candle overlap" filter rejects every real FVG** (PERCEPTION root cause, not a scoring symptom) | 2 (RISKY) → high-value | CONFIRMED 2026-06-08 via operator chart side-by-side (Bybit) + `backend/diagnostics/_fvg_probe.py`. Bot reports **0 FVGs** on BTC across 15m/1h/4h/1d while the chart has many (operator cited 6: 4h bull 61010-61609 & 62388-62844, 1d bear 70756-67590 & 66261-64580, 1d bull 61569-62508, 1h bear 63078-62912). ROOT: detection in `fvg.py` is CORRECT (3-candle `c0.high<c2.low`, 98 raw gaps found on BTC 4h alone) but the OVERLAP FILTER (`_calculate_overlap_bullish/bearish`; gate at `detect_fvgs` ~:138/:149 & :199/:201) measures how much the MIDDLE (displacement) candle overlaps the gap and rejects if > `fvg_max_overlap=0.22`. In a textbook FVG the middle candle ALWAYS spans the gap (that IS the signature) → overlap=**100%** > 22% → EVERY standard FVG rejected (`raw_passed_overlap=0`, proven). Reconciles factor_contribution "FVG fires 20%": only atypical/offset gaps survive; ~80% normal ones die. LuxAlgo (operator's reference) = 3-candle + size threshold ONLY, NO middle-candle-overlap rule → the filter is the bot's own non-standard bug. "Extend FVG:5" in LuxAlgo is a DRAW setting (extend box 5 bars), NOT candle count — bot's 3-candle count is right. IMPACT: FVGs feed the FVG scoring factor (≈dead), ENTRY ZONES, and TARGETS — a core SMC sense off on every symbol the whole time. First confirmed PERCEPTION bug from the "is the logic seeing the chart" audit. | **GATED + HIGH BLAST RADIUS.** Fix is conceptually trivial (drop the spurious overlap filter; keep 3-candle gap + `min_gap_atr` size filter = the standard/LuxAlgo definition) but flipping it makes the bot suddenly see ~50-100 FVGs/symbol/TF where it saw ~0 → floods perception→scoring→entry zones→targets that were (accidentally) calibrated FVG-blind. Needs Plan → symmetry-guard → §16 + BEFORE/AFTER measurement (does restoring FVG vision improve signal quality or just add noise the downstream weights aren't ready for?). Regression: assert `detect_fvgs` finds the known BTC gaps. NOT started. (`_fvg_probe.py` = root-cause probe; promote to a proper perception-audit diagnostic or remove.) Related open perception bugs from same BTC audit: phantom 1d `last_hl=112000` (not in detected swing set; stale pre-Dec-2025 level), 1d swings lag (stop May-6, miss June decline), `key_levels.symbol="UNKNOWN"`. |

## 🟡 IN-VALIDATION — measuring on clean data BEFORE writing any fix

| ID | Thread | Tier | Status | NEXT ACTION |
|----|--------|------|--------|-------------|
| T6 | **Entry-location / exhaustion ("dead-cat") signal** — score rates structure, blind to "entering at the spent extreme." Merges old T7. | 3 | **WEAKENING — FADED below the noise floor as data grew (2026-06-06).** Was exh_bb/loc_bb ±0.22 at n=111 (above the ±0.19 floor); at n=138 (after f193dd54 added +27 trades) it's ±0.16, BELOW the ±0.17 floor. The "strengthens with data" trend (0.19→0.20→0.21→0.22) REVERSED to 0.22→0.16. The credibility rested on it strengthening — that reversed. exh_vol popped +0.21 but wrong-signed (decel→better) = noise. The 3 motivating shorts losing was anecdote, not proof. **No longer a validated lead.** | DO NOT wire (instrumentation correctly deferred — dodged shipping noise). Keep measuring: if it keeps fading → refuted pile; if it re-firms clearly above a shrinking floor over several more sessions → reconsider. Demoted "about-to-ship" → weakening/unproven. |
| T7 | **Dead-cat detector exists but is deaf on the continuation side** — detect_pullback_setup (extension+volume-exhaustion+SMC) only ENABLES counter-trend; computes "extension favors opposite dir" then discards it for the with-trend short (pullback_detector.py:204 early-return). MERGED INTO T6 (same signal). | 3 | Root-caused this session. Live AVAX/XRP/APT shorted swept oversold support; pullback_prob only 0.465. **(a) DONE** — exhaustion proxies validated (see T6). | Wiring = T6 step 3. The fix is to feed the existing detector's output as a penalty, not to build a new detector. |
| T8 | **Edge after fees — VERDICT REACHED (the smoking gun)** | strategic | `edge_significance.py` (2026-06-06, clean n=142): journal pnl is GROSS (verified — corrects the fills-fees scope doc). **Even gross, expectancy +0.43/tr has a 95% CI [-0.66,+1.54] that SPANS ZERO (P>0=78%)** — no statistically demonstrated edge. Breakeven per-side fee 0.045% < Phemex taker 0.06% → net-NEGATIVE at taker. Full history net-negative (P>0=1%). decisions/2026-06-06__edge-after-fees-verdict.md | **Stop hunting for a predictive signal** — the gross edge isn't significant, nothing for a new input to improve. The ONLY lever on viability is EXECUTION COST: breakeven 0.045%/side sits between maker (~0.01%) and taker (0.06%) → net-positive ONLY at maker fills. Re-opens T9 (LIMIT-SNAP chase = taker = the fee problem). VERIFY-NEXT: re-run as n grows; test a maker-execution experiment. |
| T14 | **Maker-execution experiment — THE consequential lever** (edge verdict T8: only maker fills flip net-negative→positive) | strategic | DESIGN ENTRY done (decisions/2026-06-06__maker-execution-experiment-design.md, §15, paper-only). Maker path ALREADY EXISTS (pending machinery + monitor-loop fill + TTL); only the snap + immediate-fill force taker. Phase 1 = tiny default-off `execution_mode` toggle (skip snap + skip immediate fill → rest at the OB) measuring FILL RATE. | **PHASE 1 SHIPPED** (default-off `execution_mode` toggle): config field + `rest_maker_active` property (testnet-guarded) + snap-skip + immediate-fill-skip → rests at the OB; tagged on signals.jsonl + CompletedTrade; API plumbing (PaperTradingConfigRequest.execution_mode) so it's botConfig-reachable. snap_taker DEFAULT byte-inert (proven); backend-integrity CLEAN; §16 (regression test_maker_execution_toggle.py covers default-inert + testnet-forced-off). **GATE 1 (2 rest_maker sessions, n=18, fill_rate.py):** fill rate **25%** (18/71 — LOW, confirmed across both). The session-1 promise (n=9, 78% win, +5.33/tr) was a MIRAGE: session 2 regressed hard (n=9, 22% win, −6.08/tr, 8 stop-outs = the adverse-selection mode — resting shorts filled on bounces that became reversals). **Combined: 50% win, −0.38/tr GROSS, n=18.** Read: maker is ~breakeven gross — LESS negative than snap_taker (−2.15 gross), and since maker fees ≈0, maker NET beats taker NET — but it is NOT a demonstrated winner, and it does ~1/4 the trades. NO clear edge (same no-edge story in maker clothing). GATE 1 ≈ inconclusive-leaning-fail: low fill + breakeven + adverse-selection appeared. **NEXT: either accumulate more (n=18→~40 to see if it settles above/below 0) or ACCEPT "breakeven, no edge" and stop. Phase 2 (fee model) only if it trends clearly positive.** live_executor untouched. **UPDATE 2026-06-08 (session 5990edf8, overnight, power-cut at ~10:47 UTC):** +0.32 (+0.04/tr), 50% win, **4 TARGETS hit (first maker session with real target hits)**, scalp 75% / tight 0.55-ATR stops — the HEALTHY face of maker, opposite profile to the −54 swing-disaster. n now **26**, fill rate **dropped to 19%** (26/139), maker exp **−0.25 gross** (still neg, less-bad than taker −2.15). Story unchanged: breakeven-ish / low-falling-fill / no edge. n=26→40. |
| T10 | **Kill Zone Timing + Close Momentum read clean-ANTI** (high factor score → loses, on clean data) — surfaced by the T1 de-confound, now the most-persistent lead | 3 | HELD across 2 sessions where T6 faded: Kill Zone −0.24→−0.18, Close Momentum −0.19→−0.18 (n=138, vs ±0.17 floor — barely above, but not fading). | Keep measuring. OPEN QUESTION: real anti-signal (factor mis-wired/inverted) vs down-regime artifact (kill-zone timing in a one-directional market). If it holds above a shrinking floor → investigate the Kill Zone / Close Momentum scorer logic (touches scorer → symmetry-guard + §16); do NOT act on n=138 borderline alone. |

| T16 | **SMC reads LTF bearish-flush as continuation + volume-accel blind to the surge → no reversal brake** (entry-location/direction, ties R2 + T6/T7) | 3 (CORRECTNESS) | LINK trace, session 1993dcd5 (2026-06-07): the bot's OWN SMC detected the change-of-character — Market Structure factor `'Recent CHoCH [Grade B] (LTF only)'` — but scored it as bearish CONTINUATION (Multi-Candle Confirmation `'3 consecutive closes beyond CHoCH level - strong confirmation'` = score **100** toward the SHORT). The entire 24-factor stack read bearish (HTF Composite 71.8 "HTF bearish", Regime Alignment "SHORT aligned w/ down regime", BTC Impulse 100); **NO bullish read was ever generated → this is a DETECTION gap, NOT a regime override** (nothing for the gate to suppress). `volume_acceleration` read 0.03→−0.05 (flat→negative) going INTO the biggest up-candle; Volume factor said "Normal". Bot kept adding shorts at RISING confluence (70%→77%) into the rip; 6 of 8 stops fired in a 58-sec spike (22:14:28–22:15:26 UTC) = correlated alt-short book behaving as ONE bet. **`reversal_detector.py` EXISTS in `smc/` — why didn't it gate direction?** Consistent w/ R2 (location not width) + T6/T7 (no exhaustion brake). | ROOT-CAUSED (7-agent workflow wf_73fcbcdc, 2026-06-07). Decisive link = `confluence_service.py:255-265` (note: lives under `backend/services/`, NOT strategy/confluence): SHORT won a clean `score_diff<=-5.0` margin → falls through, SKIPS the only structure-aware tiebreak (`_count_recent_structure`, reachable only in the ±5 `else` band). Direction is a magnitude race between two continuation-flavored scores; nothing can vote "the leader is reversing." `reversal_detector.py` is **wired-but-not-consulted-for-direction**: only ADDS points to its own side (`confluence_service.py:710-722`, post-decision metadata), never vetoes the opposite; doubly inert via `cycle_aligned` hard-gate (`:237`). Contributing: Multi-Candle "3 closes beyond level"=flat 100 no staleness guard (`scorer.py:5912-5923`); HTF sourced from stale 1d label; volume brake needs slope>0.1 (trailing 5-bar OLS, can't lead a surge, `scorer.py:4515-4522`). Fix menu: A=opposing-reversal VETO at `:496` (needs B) · B=structure-only reversal tier (`:237/:386`, NEW input) · C=Multi-Candle staleness gate (re-weight, cheap) · D=de-couple HTF from daily (big blast). **ADVERSARIAL VERDICT (adversarial-review agent): NEEDS-MORE-DATA leaning DROP-as-scoped** — the veto keys on the T6/T7 reversal/exhaustion family that FADED below noise (n=138); in a persistent down regime a bullish-CHoCH veto would preferentially SKIP the continuation-shorts that were WORKING (counter-bounce prints a CHoCH) = adverse loss-mode swap; T8 says no edge to protect. §16 would pass it clean but can't see it's strategically mis-sequenced. | COUNTERFACTUAL RAN (`reversal_veto_counterfactual.py`, n=160 post-clamp, pre-registered gate). RESULT ≠ what builder OR skeptic predicted: the structural A+B detector is NOT what the data asks for — what separates is the simpler **counter-extreme ENTRY brake** (T6/T7 exhaustion family: don't short into oversold/lower-band; RSI 30/70 already standing). SHORT side (n=112): baseline +0.70/tr; short-into-RSI≤30 (n=20) −3.37/tr; short-into-low-band (n=51) −1.23/tr vs +2.31 KEEP; (keep−veto) CI excludes 0; aggregate gate PASSED. **BUT robustness check downgrades it:** RSI≤30 effect is **71% from ONE session (6e03d98e 2026-06-04)** = fragile/overfit (the skeptic's small-N-anecdote risk, materialized); strip it → −1.23/tr. Band form is broader (8/11 sessions net-neg for veto set) but shallow (−1.23/tr) + blunt (vetoes 45% of shorts, CI low bound +0.43). LONG side untestable (band-veto n=4, rsi-veto n=2 — down regime starved it → symmetric gate would ship on faith = skeptic's regime-flip risk CONFIRMED). Gross + in-sample. Merges with T6 (threshold/tail form separates even though T6 linear corr faded — consistent). | **🟡 WATCH-FORWARD, do NOT build.** Real-but-fragile. ONLY honest next test = OUT-OF-SAMPLE: after next overnight run, re-run `reversal_veto_counterfactual.py --since <next-session-start>` on FRESH trades only; does "short-into-lower-band underperforms" reproduce or evaporate? Watch-metric = band-extreme short veto exp vs keep exp on the new cohort. If it persists across 2-3 fresh sessions AND clears net-of-fee → THEN a §16 plan for a counter-extreme entry penalty (NOT the A+B detector). NO scorer/smc edits until forward-validated. **FORWARD ROUND 1 (--since 2026-06-08, session 5990edf8, n=8): INCONCLUSIVE, NOT refuted** — band/RSI counter-extreme triggers fired on **0/8 entries** (this session was scalps/tight-stops, NO oversold-shorts), so the hypothesis was untested, not failed. Tool auto-verdict prints "REFUTED" but that's a logic gap at n_veto=0 (conflates never-fired w/ fired-and-failed → patch the tool to distinguish). Still need a swing/oversold-heavy session to actually test. (obv_against fired 7/8 but it's the already-refuted winner-skipping trigger.) |

## 🟠 NEEDS-TRACE — found, mechanism unconfirmed (do NOT call it a bug yet)

_(none open)_

## 🟢 SHIPPED + VERIFIED (this session arc)

| ID | Thread | Refs | Verified by |
|----|--------|------|-------------|
| S1 | TP1 reachability clamp (wide-stop × 1.5R → unreachable TP1) | 99303ff | Live: febadc4a tp1_clamped=3/11, avg targets_hit 0.12→0.64 |
| S2 | Journal calc-geometry (stop branch / clamp / levels) | e6715f8 | Live: febadc4a rows carry stop_loss_rationale etc (was 0/247) |
| S3 | 8 silent-LONG `.get(chosen_direction,"LONG")` defaults → loud/UNKNOWN | 8233b21 | test_no_silent_long_default.py; live UNKNOWN rejections visible |
| S4 | **Stale bot-process / port-8001 zombie + Windows-broken `npm run kill`** (fuser→kill-port) | 3c78ec4 + killed PID 19108 | Fresh --reload backend 7:12pm; S1/S2/S3 only became live AFTER this |
| S5 | Direction-cohort backtest tooling | 819ade0 | — |
| S6 | factor_contribution + session_debrief FACTOR EDGE + entry_quality_probe | d4e1113 / 524b66c / 01677c3 | — |

## ⚫ REFUTED / CLOSED — investigated, disproven, correctly abandoned

| ID | Thread | Why closed |
|----|--------|-----------|
| R1 | **Stage-1 direction rewrite** (with-trend-only / conviction model) | Step-0 backtest: "with-trend bleeds" was ENTIRELY the wide-stop era; post-clamp both directions breakeven-noise. No losing-direction cohort to gate. decisions/2026-06-02 |
| R2 | **"Structural swing stops too wide" = the loss** | 1000FLOKI(−22) vs APT(+7) A/B: APT's stop was WIDER and won. Differentiator was entry location, not stop width. |
| R3 | **pullback_probability filter (−0.21)** | Collapses to −0.05 post-clamp — wide-stop-era confound (3rd instance of this trap). |
| R4 | **Redundant/multicollinear factors (HTF/regime/BTC triple-count)** | 0 factor pairs correlate ≥0.70 in data; a-priori claim refuted by factor_contribution. |
| R5 | **Entry-fill divergence / LIMIT SNAP is a bug or the dead-cat mechanism** (was T9) | NOT a bug: LIMIT SNAP (paper_trading_service.py:2261-2307) is by-design fill-assurance and PRESERVES risk $ (size recalc 2280-2290). snap_gap measured vs PnL on clean data (n=111) → r=+0.06 (within ±0.19 noise), big-snap trades marginally POSITIVE. Snap magnitude carries no signal; it is NOT the dead-cat mechanism (that's the band-extreme/location = T6, separable). Closed 2026-06-05. |

---

### The loop we're in (and why it's fine)
find → run bot → measure/validate → run bot → find next. Correct iteration, but it strands
"found-but-unfixed" threads (T1, T2, T3). This ledger is the backstop: **open threads don't
disappear just because we chased a deeper cause.** When a 🟡 validates, it becomes a fix task;
when a 🔴 is the highest open tier, it gets picked up before lower work (CLAUDE.md triage).
