# Local development overrides for SniperSight
# Copy to .env.local and adjust values for your environment.

# Frontend port
FRONTEND_PORT=5000

# Backend URL used by Vite proxy (default http://localhost:8000)
#BACKEND_URL=http://localhost:8000

# Bind Vite dev server host (0.0.0.0 recommended for tunnels)
HOST_BIND=0.0.0.0

# Public host for HMR when using dev tunnels (no protocol/path)
# Example: dhl9x06x-5000.usw3.devtunnels.ms
PUBLIC_HOST=

# Optional explicit HMR client port (defaults to 443 when PUBLIC_HOST is set)
#HMR_CLIENT_PORT=443

# Optional: force HMR protocol
#HMR_PROTOCOL=wss
